export class CreateUserDto {
  // Asking Form The User What Things We Are Needed From the users ...
  // and we will mapped it and saved it inside the user database ... 
  firstName: string;

  lastName: string;

  age: number;
}
