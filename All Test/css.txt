1 . Media Query : is used to make website resposiveness .... 
2. universal selctor : universol selector is a selector that matches any element type's name instead of selecting elements of a particular type ... and it is given with * 
3. css box model : the css box modle defines the layout and design of css element ... the elements are padding  , border , margin . 
4. what is the purpose of developing css : css was developed to define the visual appearance of websites. it allows developer to seperate the structure and content of a website that was not possible before ... 
5. diffrence between class and id : class : class is a way using html elements for styling and have not a unique multiple elements . whereas id is unique and it can be assigned to a single element  ...
6 . pseudo-elements : it provides special effects to some selectors . css finds use in applying styles in html markups ... it helps by allowing extra markup without interfering with the original document .. 
7. flexbox properties :  flex-direction, wrap, flow, content, and align item 
8. Realtive & absolute : raltive is used for the same tag in css. absolute is relative to the non-static parent ... 