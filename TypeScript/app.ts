// Basiclly we need to Define the type whenever we are decalring the variable .... 
function add(n1,n2) {
    return n1+n2;
}
let result = add(10,20);

console.warn(result)

// Basically we have to define the types  